export default function RoutePanel({ plan }) {
  return <section className="route-panel" aria-labelledby="routes-title"><div className="section-heading"><h2 id="routes-title">Compare your options</h2><span className="eyebrow">ILLUSTRATIVE PLAN</span></div><div className="route-grid">{plan.routes.map((route, i) => <article className={`route-card ${route.type}`} key={i}><div className="route-title"><span className="route-line" /><h3>{route.type === 'recommended' ? 'Recommended' : 'Fastest alternative'}</h3></div><p className="metrics"><strong>{route.travel_time_min}<small> min</small></strong><span>{route.distance_km} km</span></p><p>{route.named_roads.join(' → ')}</p><p className="exposure">{route.type === 'recommended' ? 'Lower modeled exposure' : '△ Greater modeled exposure'} <strong>{route.exposure.toFixed(3)}</strong></p><details><summary>Exposure by modeled region</summary>{Object.entries(route.exposure_breakdown_km).filter(([band]) => band !== 'smoke').map(([band, km]) => <p key={band}>{band} region: {Number(km.toFixed(3))} km</p>)}<small>Regions are cumulative; do not add these distances.</small>{route.exposure_breakdown_km.smoke > 0 && <p className="smoke-line">Modeled smoke plume: {Number(route.exposure_breakdown_km.smoke.toFixed(3))} km — overlaps the regions above rather than adding to them, and is the burning footprint swept along forecast wind, not measured air quality.</p>}</details></article>)}</div>{plan.warnings.map(warning => <p className="route-warning" key={warning}>{warning}</p>)}</section>
}
