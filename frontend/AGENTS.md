# Frontend agent rules

- Only edit frontend/ (and backend/agent/ for agent code).
- Never edit contracts/ without the team agreeing first.
- Start from demo_data/ fake files; don't block on real endpoints.
- Legend says "modeled risk regions", never "fire boundary".
- Always show data timestamps and the disclaimer: "Experimental decision-support tool. Follow official evacuation orders if they differ."
- No API keys in frontend code.
- Draw bands largest first (h6 → h3 → h1 → current).